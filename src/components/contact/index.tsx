import * as React from "react";
import styled from "styled-components";

export const Contact: React.FC = () => {
  return <ContactWrap id="contact">Contact</ContactWrap>;
};

const ContactWrap = styled.section`
  width: 100%;
  height: 80vh;
  // background: #63c132;
`;
